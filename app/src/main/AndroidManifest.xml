<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aprr.biotracer">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen"
            android:label="@string/app_name"
            android:noHistory="true"
            android:screenOrientation="portrait"
            android:theme="@style/Splashscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <!-- <activity android:name=".SplashScreen"></activity> -->

        <activity android:name=".alumni.AlumniActivity" />
        <activity android:name=".PetunjukActivity" />
        <activity android:name=".pengguna.PenggunaActivity" />
        <activity android:name=".BantuanActivity" />
        <activity android:name=".ProfilActivity" />
        <activity android:name=".MainPenggunaActivity" />
        <activity
            android:name=".LoginActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name=".RegisterActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".ProfilData.ProfilAddActivity" />
        <activity android:name=".ProfilPenggunaActivity" />
        <activity android:name=".ProfilData.ProfilPenggunaAddActivity" />
        <activity android:name=".alumni.AlumniEssayActivity" />
        <activity android:name=".alumni.AlumniMakulBergunaActivity" />
        <activity android:name=".alumni.SaranAlumniActivity" />
        <activity android:name=".pengguna.PenggunaQuestActivity" />
        <activity android:name=".pengguna.PenggunaEssayActivity" />
        <activity android:name=".alumni.HasilAlumniActivity" />
        <activity android:name=".pengguna.HasilPenggunaActivity" />
        <activity android:name=".alumni.AlumniMakulPerluActivity" />
        <activity android:name=".alumni.AlumniMakulSulitActivity" />
        <activity android:name=".ProfilData.ProfilUbahActivity"></activity>
    </application>

</manifest>